Add this folder (with subfolders) to your matlab path
Also add SPM12b to your path (without subfolders)

To run longitudinal processing
Set up various paths to your data in LONG_config
Then open LONG_setup and evaluate each section as needed.
